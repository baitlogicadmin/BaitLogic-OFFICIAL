"use strict";
const $=s=>document.querySelector(s),set=(el,value)=>{if(el)el.textContent=value;};
const esc=value=>String(value??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const clock=value=>value?new Date(value).toLocaleTimeString([],{hour:"numeric",minute:"2-digit"}):"--";
const E={air:$("#airTemperature"),weather:$("#weatherCondition"),loc:$("#locationName"),locDetail:$("#locationDetail"),gpsState:$("#gpsState"),gpsAccuracy:$("#gpsAccuracy"),dataState:$("#dataState"),sourceState:$("#sourceState"),pressure:$("#pressureValue"),pressureShort:$("#pressureShort"),trend:$("#pressureTrend"),delta:$("#pressureDelta"),bite:$("#biteStatus"),updated:$("#lastUpdated"),locStatus:$("#locationStatus"),dot:$("#locationStatusDot"),line:$("#pressureTrendLine"),refresh:$("#refreshConditions"),retry:$("#retryLocation"),connection:$("#connectionStatus"),connectionPill:$("#connectionPill"),wind:$("#windValue"),windDir:$("#windDirection"),cloud:$("#cloudValue"),light:$("#lightState"),catches:$("#recentCatches"),open:$("#openCatchLogger"),bottomLog:$("#bottomLogButton"),bottomRefresh:$("#bottomRefreshButton"),modal:$("#catchLoggerModal"),close:$("#closeCatchLogger"),cancel:$("#cancelCatch"),form:$("#catchForm"),weight:$("#catchWeight"),plus:$("#increaseWeight"),minus:$("#decreaseWeight"),catchLoc:$("#catchLocation"),useLoc:$("#useCatchLocation"),activity:$("#fishActivity"),mood:$("#fishMood"),tactic:$("#tacticAdvice"),activityReason:$("#activityReason"),lure:$("#lureFamily"),lureReason:$("#lureReason"),zone:$("#targetZone"),habitat:$("#habitatShift"),windows:$("#biteWindows"),windowReason:$("#windowReason"),species:$("#speciesMatch"),speciesReason:$("#speciesReason"),confidence:$("#confidenceScore"),confidenceText:$("#confidenceText"),alert:$("#patternAlert"),alertReason:$("#patternReason"),decision:$("#primaryDecision"),decisionReason:$("#decisionReason"),sigPressure:$("#signalPressure"),sigWind:$("#signalWind"),sigSky:$("#signalSky"),sigLocation:$("#signalLocation"),install:$("#installButton")};
const S={lat:null,lon:null,accuracy:null,place:null,weather:null,live:false,locationAttempt:0,dataAttempt:0};
const WEATHER={0:"Clear",1:"Mostly clear",2:"Partly cloudy",3:"Cloudy",45:"Fog",48:"Fog",51:"Light drizzle",53:"Drizzle",55:"Heavy drizzle",61:"Light rain",63:"Rain",65:"Heavy rain",71:"Light snow",73:"Snow",75:"Heavy snow",80:"Rain showers",81:"Rain showers",82:"Heavy showers",95:"Thunderstorms",96:"Thunderstorms / hail",99:"Severe thunderstorms / hail"};
const CACHE_KEY="baitlogic-barometer-last-v1",CACHE_MAX_AGE=86400000;
function online(){
  if(!navigator.onLine)set(E.connection,"Offline");
  else if(S.live)set(E.connection,"Live");
  else if(S.weather)set(E.connection,"Saved");
  else if(!/Loading|Locating|Unavailable/i.test(E.connection?.textContent||""))set(E.connection,"Online");
}
function status(text,kind=""){set(E.locStatus,text);if(E.dot){E.dot.classList.remove("locked","error");if(kind)E.dot.classList.add(kind)}}
function compass(deg){if(!Number.isFinite(deg))return"Direction --";const dirs=["N","NE","E","SE","S","SW","W","NW"];return`${dirs[Math.round(deg/45)%8]} • ${Math.round(deg)}°`}
function classifyTrend(d3,d6){if(d3<=-.12||d6<=-.20)return"falling_fast";if(d3<=-.03||d6<=-.08)return"falling";if(d3>=.12||d6>=.20)return"rising_fast";if(d3>=.03||d6>=.08)return"rising";return"steady"}
function trendStory(t){return({falling_fast:"Pressure is dropping quickly. Treat this as a changing window and verify fish activity fast.",falling:"Pressure is falling. Search range may expand, but local fish behavior still decides the pattern.",steady:"Pressure is stable. Cover, depth, light and local water evidence matter more than chasing pressure.",rising:"Pressure is rising. Tighten presentation and prioritize high-percentage cover/depth intersections.",rising_fast:"Pressure is rising quickly. Expect the previous pattern to become less reliable."})[t]}
function fail(message,{location=false}={}){set(E.dataState,"Live data unavailable");set(E.updated,"Tap Retry to try again");set(E.decision,"RETRY");set(E.tactic,location?"Location is needed to load verified local conditions.":"Live conditions could not be verified.");set(E.decisionReason,message);set(E.bite,"NO LIVE DATA");set(E.confidence,"0");set(E.confidenceText,"BaitLogic withholds the recommendation when current data cannot be verified.");set(E.connection,navigator.onLine?"Unavailable":"Offline");if(location){set(E.loc,"Location needed");set(E.locDetail,message)}status("Live conditions unavailable","error");if(E.refresh)E.refresh.disabled=false}
function softFail(message,{location=false}={}){if(!S.weather)return fail(message,{location});S.live=false;set(E.connection,navigator.onLine?"Saved":"Offline");set(E.dataState,"Saved conditions");set(E.updated,"Refresh pending");if(location)set(E.locDetail,`${message} Showing the last verified area.`);status("Showing saved conditions — refresh pending","error");if(E.refresh)E.refresh.disabled=false}
function readCached(){try{const cached=JSON.parse(localStorage.getItem(CACHE_KEY)||"null");if(!cached||Date.now()-Number(cached.savedAt)>CACHE_MAX_AGE||!Number.isFinite(Number(cached.lat))||!Number.isFinite(Number(cached.lon))||!Number.isFinite(Number(cached.snapshot?.weather?.pressureInHg)))return null;return cached}catch{return null}}
function saveCached(snapshot){try{if(!Number.isFinite(S.lat)||!Number.isFinite(S.lon))return;localStorage.setItem(CACHE_KEY,JSON.stringify({savedAt:Date.now(),lat:Number(S.lat.toFixed(2)),lon:Number(S.lon.toFixed(2)),accuracy:Math.max(Number(S.accuracy)||0,1500),snapshot}))}catch{}}
function restoreCached(){const cached=readCached();if(!cached)return null;S.lat=cached.lat;S.lon=cached.lon;S.accuracy=cached.accuracy;render(cached.snapshot,{cached:true});return cached}
async function fetchWithTimeout(url,ms=12000){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),ms);try{const r=await fetch(url,{cache:"no-store",signal:controller.signal});let d={};try{d=await r.json()}catch{}if(!r.ok)throw new Error(d.error||`Request failed (${r.status})`);return d}finally{clearTimeout(timer)}}
function safetyDecision(w,alerts){const severe=(alerts||[]).find(a=>["Extreme","Severe"].includes(a.severity)||/warning/i.test(a.event||""));if([95,96,99].includes(w.code))return{word:"STOP • SEEK SHELTER",reason:"Thunderstorm conditions are present. Do not use a fishing score to override lightning safety.",score:15,mood:"Unsafe"};if(severe)return{word:"CHECK OFFICIAL WARNING",reason:`Active NWS alert: ${severe.event}. Review the official warning before heading out.`,score:25,mood:"Alert"};let score=58,reasons=[];const trend=classifyTrend(w.pressureDelta3h,w.pressureDelta6h);if(trend==="falling_fast"){score+=17;reasons.push("fast-falling pressure")}else if(trend==="falling"){score+=10;reasons.push("falling pressure")}else if(trend==="steady"){score+=4;reasons.push("stable pressure")}else if(trend==="rising"){score-=5;reasons.push("rising pressure")}else{score-=10;reasons.push("fast-rising pressure")}if(w.windMph>=4&&w.windMph<=13)score+=6;else if(w.windMph>22){score-=10;reasons.push("strong wind")}if(w.gustMph>30){score-=8;reasons.push("high gusts")}if(w.cloudCover>=35&&w.cloudCover<=90)score+=5;if(w.precipitationIn>.15){score-=6;reasons.push("active precipitation")}if(w.apparentTemperatureF>=100){score-=18;reasons.push("dangerous heat stress")}else if(w.apparentTemperatureF>=90){score-=8;reasons.push("high heat")}score=Math.max(20,Math.min(92,Math.round(score)));return{word:score>=76?"FAVORABLE":score>=58?"FISHABLE • ADAPT":"SELECTIVE",reason:reasons.length?`Main live signals: ${reasons.join(", ")}. Verify the pattern with actual fish behavior and local water conditions.`:"Current signals are workable. Verify depth, cover and presentation before assuming a pattern.",score,mood:score>=76?"Active":score>=58?"Mixed":"Holding"}}
function render(d,{cached=false}={}){const w=d.weather;S.weather=w;S.live=!cached;set(E.connection,cached?"Saved":"Live");if(d.location?.name){S.place=d.location.name;set(E.loc,S.place);set(E.locDetail,cached?"Saved verified area • refreshing now":"Live device location • place name verified");if(E.catchLoc)E.catchLoc.value=S.place}else{set(E.loc,"Current location");set(E.locDetail,cached?"Saved verified area • refreshing now":"GPS locked • place name unavailable")}const trend=classifyTrend(w.pressureDelta3h,w.pressureDelta6h),decision=safetyDecision(w,d.alerts||[]);set(E.air,`${Math.round(w.temperatureF)}°`);set(E.weather,`${WEATHER[w.code]||"Current conditions"} • feels ${Math.round(w.apparentTemperatureF)}°`);set(E.wind,`${Math.round(w.windMph)} mph`);set(E.windDir,`${compass(w.windDirection)} • gusts ${Math.round(w.gustMph||0)} mph`);set(E.cloud,`${Math.round(w.cloudCover)}%`);set(E.light,w.isDay?"Daylight":"Low light / night");set(E.pressure,Number(w.pressureInHg).toFixed(2));set(E.pressureShort,`${trend.replace("_"," ")} • ${w.pressureDelta3h>=0?"+":""}${Number(w.pressureDelta3h).toFixed(2)} inHg / 3h`);set(E.trend,trendStory(trend));set(E.delta,`3h ${w.pressureDelta3h>=0?"+":""}${Number(w.pressureDelta3h).toFixed(2)} • 6h ${w.pressureDelta6h>=0?"+":""}${Number(w.pressureDelta6h).toFixed(2)} inHg`);set(E.bite,decision.word);set(E.activity,decision.score);set(E.mood,decision.mood);set(E.decision,decision.word);set(E.decisionReason,decision.reason);let tactic="Fish a proven depth/cover combination first, then let bites, follows and bait activity decide the next move.",lure="Confidence bait + finesse follow-up",zone="Cover beside a depth transition",alert="PATTERN NEEDS PROOF",alertReason="Conditions are only inputs. Local fish response is the deciding evidence.";if(trend.includes("falling")){tactic="Search efficiently with a moving bait, then follow contacts with a slower presentation.";lure="Moving bait → precision follow-up";zone="Wind-contact edges + travel lanes";alert="WINDOW MAY BE OPENING";alertReason="Falling pressure can support broader movement, but it is not a guarantee of feeding activity."}if(trend.includes("rising")){tactic="Reduce water coverage and make precise casts where cover intersects the first useful depth change.";lure="Compact jig / worm / subtle profile";zone="Tight cover + first break";alert="POSITION MAY TIGHTEN";alertReason="Rising pressure can compress the pattern; verify before committing."}if(w.windMph>20)zone="Protected structure + controllable wind";if([95,96,99].includes(w.code)){tactic="Stop fishing and seek appropriate shelter.";lure="—";zone="Safe shelter"}set(E.tactic,tactic);set(E.activityReason,`Pressure ${trend.replace("_"," ")} • ${Math.round(w.windMph)} mph wind • ${Math.round(w.cloudCover)}% clouds • feels ${Math.round(w.apparentTemperatureF)}°F`);set(E.lure,lure);set(E.lureReason,"A starting presentation based on current conditions, not a promise of fish activity.");set(E.zone,zone);set(E.habitat,"Use local structure, cover, water clarity and depth to refine this starting zone.");const am=w.sunrise?`${clock(new Date(new Date(w.sunrise).getTime()-1800000))}–${clock(new Date(new Date(w.sunrise).getTime()+5400000))}`:"--",pm=w.sunset?`${clock(new Date(new Date(w.sunset).getTime()-5400000))}–${clock(new Date(new Date(w.sunset).getTime()+1800000))}`:"--";set(E.windows,`AM ${am} • PM ${pm}`);set(E.windowReason,"Daylight windows are timing context only; live weather, water and actual fish response matter more.");set(E.species,"Bass • Crappie • Panfish");set(E.speciesReason,"General condition-fit starting group. Seasonal biology and the actual waterbody must refine this.");const confidence=Math.max(35,Math.min(95,Math.round(94-(S.accuracy||250)/15-((d.alerts||[]).length?5:0))));set(E.confidence,confidence);set(E.confidenceText,cached?"Saved verified conditions are shown while BaitLogic refreshes. They are not labeled as live.":`Verified BaitLogic server response + live GPS ±${Math.round(S.accuracy||0)} m. Recommendation remains guidance, not a safety guarantee.`);set(E.alert,alert);set(E.alertReason,alertReason);set(E.sigPressure,`${trend.replace("_"," ")} • ${w.pressureDelta3h>=0?"+":""}${Number(w.pressureDelta3h).toFixed(2)} inHg / 3h`);set(E.sigWind,`${Math.round(w.windMph)} mph ${compass(w.windDirection).split(" • ")[0]} • gusts ${Math.round(w.gustMph||0)}`);set(E.sigSky,`${WEATHER[w.code]||"Current sky"} • ${Math.round(w.cloudCover)}% clouds`);set(E.dataState,cached?"Saved conditions":"Live conditions loaded");set(E.sourceState,d.source?.alerts==="National Weather Service"?"Open-Meteo + NWS":"Open-Meteo");set(E.updated,cached?`Saved ${new Date(d.updatedAt||Date.now()).toLocaleTimeString([],{hour:"numeric",minute:"2-digit"})}`:new Date(d.updatedAt||Date.now()).toLocaleTimeString([],{hour:"numeric",minute:"2-digit"}));status(cached?"Showing saved conditions — refreshing":"Live intelligence ready",cached?"":"locked");if(!cached)saveCached(d);if(E.refresh)E.refresh.disabled=false}
async function load(){if(S.lat===null||S.lon===null)return locate();const attempt=++S.dataAttempt,lat=S.lat,lon=S.lon;if(E.refresh)E.refresh.disabled=true;set(E.connection,S.weather?"Saved":"Loading live data");set(E.dataState,S.weather?"Saved conditions • refreshing":"Fetching live conditions");set(E.updated,S.weather?"Refreshing…":"Working…");status("Reading BaitLogic live data");try{const d=await fetchWithTimeout(`/api/barometer-snapshot?lat=${encodeURIComponent(lat)}&lon=${encodeURIComponent(lon)}`,12000);if(attempt===S.dataAttempt)render(d)}catch(error){if(attempt===S.dataAttempt)softFail(error.name==="AbortError"?"Live conditions timed out. Tap Retry to try again.":error.message||"Live conditions could not be loaded.")}}
function locate(){
  const attempt=++S.locationAttempt;
  if(!navigator.geolocation){softFail("This browser does not provide location access.",{location:true});return}
  set(E.connection,S.weather?"Saved":"Locating");set(E.gpsState,"Requesting location");if(!S.weather){set(E.dataState,"Waiting for location");set(E.decision,"LOCATING");set(E.loc,"Finding your location…");set(E.locDetail,"Using a fast device location fix")}status("Requesting your current location");
  const watchdog=setTimeout(()=>{
    if(attempt!==S.locationAttempt)return;
    S.locationAttempt++;
    set(E.gpsState,"Location timed out");set(E.gpsAccuracy,"No coordinates used");
    softFail("Location took too long. Check Chrome site permissions and tap Relocate.",{location:true});
  },10000);
  navigator.geolocation.getCurrentPosition(p=>{
    if(attempt!==S.locationAttempt)return;
    clearTimeout(watchdog);S.lat=p.coords.latitude;S.lon=p.coords.longitude;S.accuracy=p.coords.accuracy;set(E.gpsState,"Location locked");set(E.gpsAccuracy,`±${Math.round(S.accuracy)} m accuracy`);set(E.sigLocation,S.accuracy<=50?`Strong GPS • ±${Math.round(S.accuracy)} m`:S.accuracy<=150?`Usable GPS • ±${Math.round(S.accuracy)} m`:`Broad GPS • ±${Math.round(S.accuracy)} m`);set(E.loc,"Identifying place…");set(E.locDetail,"Location locked • retrieving town name");status("Location locked — loading conditions","locked");load();
  },err=>{
    if(attempt!==S.locationAttempt)return;
    clearTimeout(watchdog);set(E.gpsState,"Location unavailable");set(E.gpsAccuracy,"No coordinates used");
    const message=err.code===1?"Location permission is blocked. In Chrome, allow Location for bait-logic.com, then tap Relocate.":err.code===3?"Location timed out. Turn on device Location and tap Relocate.":"Your device could not provide a location. Turn on Location and tap Relocate.";
    softFail(message,{location:true});
  },{enableHighAccuracy:false,timeout:9000,maximumAge:300000});
}
async function loadCatches(){if(!E.catches)return;try{const d=await fetchWithTimeout("/api/catches",8000),rows=(d.catches||[]).slice(0,6);E.catches.innerHTML=rows.length?rows.map(c=>`<li><strong>${esc(c.species)}</strong><span>${c.weight!=null?`${Number(c.weight).toFixed(1)} lb • `:""}${esc(c.location||"Location not shared")}</span></li>`).join(""):'<li class="catches-empty">No catches logged yet.</li>'}catch{E.catches.innerHTML='<li class="catches-empty">Catch feed unavailable right now.</li>'}}
function openModal(){if(E.modal)E.modal.hidden=false}function closeModal(){if(E.modal)E.modal.hidden=true}function wireCatchLogger(){E.open?.addEventListener("click",openModal);E.bottomLog?.addEventListener("click",openModal);E.close?.addEventListener("click",closeModal);E.cancel?.addEventListener("click",closeModal);E.plus?.addEventListener("click",()=>{E.weight.value=(Number(E.weight.value||0)+.1).toFixed(1)});E.minus?.addEventListener("click",()=>{E.weight.value=Math.max(0,Number(E.weight.value||0)-.1).toFixed(1)});E.useLoc?.addEventListener("click",()=>{if(E.catchLoc)E.catchLoc.value=S.place||"Current location"});E.form?.addEventListener("submit",async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(E.form).entries());try{const r=await fetch("/api/catches",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)}),d=await r.json();if(!r.ok)throw new Error(d.error||"Could not save catch");closeModal();E.form.reset();if(E.weight)E.weight.value="1";loadCatches()}catch(err){alert(err.message)}})}
let installPrompt=null;window.addEventListener("beforeinstallprompt",e=>{e.preventDefault();installPrompt=e;if(E.install)E.install.hidden=false});E.install?.addEventListener("click",async()=>{if(!installPrompt)return;installPrompt.prompt();await installPrompt.userChoice;installPrompt=null;E.install.hidden=true});E.connectionPill?.addEventListener("click",()=>S.lat!=null?load():locate());E.refresh?.addEventListener("click",()=>load());E.retry?.addEventListener("click",locate);E.bottomRefresh?.addEventListener("click",()=>load());window.addEventListener("online",()=>{online();if(S.lat!=null)load()});window.addEventListener("offline",online);online();wireCatchLogger();loadCatches();const cached=restoreCached();if(cached&&navigator.onLine)load();locate();if("serviceWorker"in navigator)navigator.serviceWorker.register("/sw.js").catch(()=>{});
