{
  "name": "baitlogic-deployable",
  "version": "1.0.0",
  "private": true,
  "description": "BaitLogic Outdoors mobile-first community intelligence PWA",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "npm --prefix mobile-app run check:readiness",
    "check:readiness": "npm --prefix mobile-app run check:readiness",
    "test:legacy-api": "node --test tests/server.test.js"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "helmet": "^8.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.4.0"
  }
}
